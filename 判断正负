#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<string.h>

int main()
{
	int n;
	scanf("%d", &n);

	if (n == 0) {
		printf("zero");
	}
	else {
		char ch[10] = {0};
		strcpy(ch,(n > 0 ? "positive" : "negative"));
		printf("%s", ch);
	}
	return 0;
}

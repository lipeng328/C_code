#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int main()
{
    int a, b;
    scanf("%d %d", &a, &b);
    if (a < 1 || a > 6 || b < 1 || b > 6)
    {
        printf("错误");
    }
    else {
        int c = (a >= b ? a : b);
        int arr[] = { 1, 2, 3, 4, 5, 6 };
        int i = 0;
        for (i = 0; i < 6; i++) {
            if (arr[i] >= c) {
                printf("%d ", arr[i]);
            }
        }
    }
    return 0;
}

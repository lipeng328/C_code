#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int main()
{
	int a = 1, b, result;

	for (; a < 10; a++) {
		for (b = a; b <= 9; b++) {
			result = a * b;
			printf("%d * %d = %d\t", a, b, result);
		}
		printf("\n");
	}

	return 0;
}
